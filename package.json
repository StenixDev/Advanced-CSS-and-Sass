{
  "name": "html-sass-project",
  "version": "1.0.0",
  "description": "Simple HTML + SCSS setup",
  "scripts": {
    "build": "sass style.scss style.css",
    "watch": "sass --watch sass/main.scss css/style.css"
  },
  "devDependencies": {
    "sass": "^1.81.0"
  }
}
